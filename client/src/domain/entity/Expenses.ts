export default class Expenses {

    constructor (
        readonly codigo: string,
        readonly lojista: string,
        readonly shopping: string,
        readonly especialista: string,
        readonly montante: string,
        readonly maiorAtras: string
    ) {
    }

    getTotal () {
        this.expenses.
    }
}
